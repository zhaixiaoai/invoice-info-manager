# v1.4 更新部署说明

## 一、先升级 Supabase 数据库

进入 Supabase 项目，打开 `SQL Editor`，新建查询，将文件：

`supabase/migration-v1.4.sql`

里的全部内容复制进去，点击 `Run`。

看到 `Success` 后再上传代码。该脚本不会删除已有开票信息，主要完成：

- 取消税号唯一限制；
- 新增独立成员账号表；
- 新增访问记录表。

## 二、上传代码到 GitHub

将压缩包解压，把里面全部文件和文件夹上传到仓库：

`zhaixiaoai/invoice-info-manager`

覆盖同名文件并提交。建议提交名称：

`增加独立成员账号、访问记录并修复重复保存提示`

## 三、等待 Vercel 自动部署

进入 Vercel 的 `Deployments`，等待最新一条显示：

`Ready / Latest / Production`

打开正式网址后按 `Ctrl + F5` 强制刷新。

## 四、管理员登录并创建成员

管理员登录方式不变：

- 第一栏填写管理员姓名；
- 第二栏填写 `ADMIN_PASSWORD`。

进入后点击“成员管理”，为每位在职人员分别创建：

- 成员账号；
- 成员姓名；
- 个人口令。

以后成员不再使用公共 `TEAM_PASSWORD`。旧的 `TEAM_PASSWORD` 已不再用于登录。

员工离职时，在“成员管理”里点击“停用账号”。停用后，该成员已登录的手机和电脑会在下一次请求时立即退出，不能继续查看。

## 五、访问记录说明

管理员点击“访问记录”可以看到：

- 谁在什么时间登录；
- 哪位成员在什么时间查看了哪家公司；
- 哪位成员在什么时间复制了哪家公司的开票信息。

“查看信息”的记录触发规则：共享成员的公司卡片至少有一半进入浏览器可视区域，同一登录期间同一家公司只记录一次，避免每10秒同步产生重复记录。

访问记录从 v1.4 上线后开始产生，无法补录旧版本历史记录。

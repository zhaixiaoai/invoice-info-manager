# 部署说明（不需要编写代码）

## 一、把本项目文件上传到 GitHub

打开 `zhaixiaoai/invoice-info-manager` 仓库：

1. 点击 **Add file**。
2. 点击 **Upload files**。
3. 将本项目压缩包解压后，把里面的全部文件和文件夹拖进上传区域。
4. 页面底部填写说明：`初始化云端多人共享版`。
5. 点击 **Commit changes**。

> 上传的是压缩包里面的文件，不是只上传 ZIP 文件本身。

## 二、创建 Supabase 云数据库

1. 注册并登录 Supabase。
2. 点击 **New project**，创建新项目。
3. 项目名称可填写 `invoice-info-manager`。
4. 数据库密码请单独保存，不要发给他人。
5. 等待项目创建完成。

### 建表

1. 在 Supabase 左侧点击 **SQL Editor**。
2. 点击 **New query**。
3. 打开本仓库的 `supabase/schema.sql`，复制全部内容。
4. 粘贴到 SQL Editor。
5. 点击 **Run**。
6. 出现成功提示即完成。

## 三、复制 Supabase 配置信息

在 Supabase 项目中：

1. 从项目的 **Connect** 或 **Settings → API** 中复制 **Project URL**，作为 `SUPABASE_URL`。
2. 进入 **Settings → API Keys**。
3. 创建或复制 **Secret key**，通常以 `sb_secret_` 开头，作为 `SUPABASE_SECRET_KEY`。

重要：Secret key 拥有完整数据库权限，只能放入 Vercel 环境变量；不要上传到 GitHub，不要发截图。

## 四、部署到 Vercel 固定网址

1. 注册并登录 Vercel。
2. 点击 **Add New → Project**。
3. 连接 GitHub，并选择 `invoice-info-manager` 仓库。
4. Framework Preset 保持 **Other** 或自动识别。
5. 展开 **Environment Variables**，依次添加：

| 名称 | 填写内容 |
|---|---|
| `SUPABASE_URL` | Supabase Project URL |
| `SUPABASE_SECRET_KEY` | Supabase Secret key |
| `TEAM_PASSWORD` | 自己设置的团队访问口令，建议 12 位以上 |
| `SESSION_SECRET` | 自己设置的 40 位以上随机字符串 |

`SESSION_SECRET` 可自行使用一长串随机字母和数字，例如至少 40 位；它不是团队登录口令。

6. 点击 **Deploy**。
7. 部署完成后，Vercel 会显示固定网址。把这个网址分享给团队。

## 五、以后怎样更新

以后只需要修改 GitHub 仓库中的网页代码。Vercel 会自动部署新版，固定网址不变。数据库独立保存在 Supabase，正常更新页面不会清空历史资料。

不要在更新时执行 `drop table invoice_companies`、删除 Supabase 项目或更换数据库环境变量。

## 六、第一次迁移旧工具的数据

1. 在原本地 HTML 工具中点击 **导出备份**。
2. 打开新云端网址并登录。
3. 点击 **导入备份**。
4. 选择原工具导出的 JSON 文件。
5. 导入成功后，不同设备都能看到相同数据。

## 七、备份建议

- 工具内定期点击“导出备份”。
- Supabase 付费方案提供自动日备份；免费方案仍建议保留离线 JSON 备份。
- 不要删除 Supabase 项目。删除项目会同时删除数据库和平台备份。

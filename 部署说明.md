# v1.3 更新部署说明

## 1. 上传代码

将压缩包解压，把里面的全部文件和文件夹上传到 GitHub 仓库 `zhaixiaoai/invoice-info-manager` 根目录，覆盖同名文件并提交。

建议提交名称：

`修复回收站按钮、复制空字段并增加只读权限`

## 2. 新增管理员环境变量

进入 Vercel 项目：

`invoice-info-manager → Settings → Environment Variables`

新增：

| 名称 | 用途 |
|---|---|
| `ADMIN_PASSWORD` | 管理员本人使用的管理口令，至少 8 位 |

注意：

- `ADMIN_PASSWORD` 必须与现有 `TEAM_PASSWORD` 不同。
- 现有 `TEAM_PASSWORD` 以后作为共享成员的只读查看口令。
- 管理员口令不要发给其他人。

## 3. 重新部署

保存环境变量后进入 `Deployments`，对最新 Production 部署执行 `Redeploy`，等待状态显示 `Ready / Latest`。

## 4. 重新登录

旧会话会按只读权限处理。管理员部署完成后应点击“退出”，再用 `ADMIN_PASSWORD` 登录。

共享给其他人时，只发送：

- 固定网址
- `TEAM_PASSWORD`

不要发送 `ADMIN_PASSWORD`、Supabase 密钥或 Vercel 登录信息。

## 5. 验收

- 管理员登录后可看到新增、编辑、删除、回收站、导入和导出按钮。
- 只读成员登录后只看到刷新、搜索和复制按钮。
- 公司列表提示条右侧“查看回收站”按钮可正常进入回收站。
- 未填写注册电话或备注时，复制内容中不出现对应空白行。

本次更新无需重新执行数据库脚本，不会删除已有数据。

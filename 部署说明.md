# v1.5 部署说明

已有项目升级时，只运行 `supabase/migration-v1.5.sql`。该脚本为累计升级脚本，已经运行过 v1.4 也可以再次执行，不会删除现有开票信息。

数据库执行成功后，把解压后的全部文件上传到 GitHub 仓库根目录，提交后等待 Vercel 最新 Production 部署显示 `Ready / Latest`。

管理员重新打开正式网址并按 `Ctrl + F5`，进入“成员管理”，点击成员右侧的“设置查看权限”。

- 查看全部公司：当前全部公司和以后新增公司自动可见；
- 只查看指定公司：只显示勾选的公司，可随时增加或取消。

成员账号停用、口令重置和访问记录功能保持不变。

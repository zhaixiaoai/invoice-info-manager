# v1.3 修复说明

本版本包含以下调整：

1. 修复公司列表提示条右侧“查看回收站”按钮无响应的问题；该按钮与顶部“回收站”按钮均可正常切换。
2. 复制全部信息时，仅复制已填写项目。注册电话、备注未填写时，不再复制空白字段。
3. 新增管理员与只读成员权限：
   - 管理员：查询、复制、新增、编辑、删除、恢复、导入、导出。
   - 只读成员：仅查询、刷新和复制当前有效的开票信息。
4. 权限同时在前端界面和后端接口执行，不能通过直接调用接口绕过。
5. 保留 v1.1 的弹窗顶部错误提示，以及删除记录重新录入时自动恢复原记录的处理。

## 上线前必须新增环境变量

在 Vercel 项目中新增：

- `ADMIN_PASSWORD`：仅管理员本人知道的管理口令，至少 8 位，且不能与 `TEAM_PASSWORD` 相同。

现有 `TEAM_PASSWORD` 将作为共享成员的只读查看口令。

保存环境变量后重新部署。部署完成后，管理员需要退出旧会话，再使用 `ADMIN_PASSWORD` 重新登录。

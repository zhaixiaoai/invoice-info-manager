# v1.5 更新部署说明

## 一、运行数据库累计升级脚本

进入 Supabase 项目，打开 `SQL Editor`，把当前编辑区旧内容全部删除，只粘贴：

`supabase/migration-v1.5.sql`

点击 `Run`，看到 `Success` 后继续。

该脚本可重复执行。无论 v1.4 是否已运行，都可以直接运行 v1.5，不会删除现有开票信息。

## 二、上传代码到 GitHub

将压缩包解压，把里面全部文件和文件夹上传到：

`zhaixiaoai/invoice-info-manager`

覆盖同名文件并提交。建议提交名称：

`增加成员按公司查看权限`

## 三、等待 Vercel 自动部署

进入 Vercel 的 `Deployments`，等待最新一条显示：

`Ready / Latest / Production`

打开正式网址后按 `Ctrl + F5` 强制刷新。

## 四、设置成员权限

管理员登录后：

1. 点击“成员管理”；
2. 找到对应成员；
3. 点击“设置查看权限”；
4. 选择“查看全部公司”或“只查看指定公司”；
5. 指定模式下勾选允许查看的公司并保存。

权限保存后立即生效。成员下一次刷新、搜索或自动同步时，只会收到有权限的公司信息。
